<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Điểm danh hôm nay</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg: #f3f4f6;
      --card-bg: #ffffff;
      --primary: #2563eb;
      --primary-soft: #dbeafe;
      --text-main: #111827;
      --text-sub: #6b7280;
      --border: #e5e7eb;
      --radius-lg: 16px;
      --radius-md: 10px;
      --shadow-soft: 0 10px 25px rgba(15, 23, 42, 0.08);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
        sans-serif;
      background: radial-gradient(circle at top, #e0f2fe 0, #f3f4f6 55%);
      color: var(--text-main);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      padding: 24px;
    }

    .page {
      width: 100%;
      max-width: 900px;
      background: rgba(255, 255, 255, 0.9);
      border-radius: 24px;
      padding: 24px 20px 28px;
      box-shadow: var(--shadow-soft);
      backdrop-filter: blur(10px);
    }

    @media (min-width: 768px) {
      .page {
        padding: 28px 32px 32px;
      }
    }

    .page-header {
      display: flex;
      flex-direction: column;
      gap: 6px;
      margin-bottom: 20px;
    }

    .page-header-top {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
    }

    .page-title {
      font-size: 1.8rem;
      font-weight: 700;
      letter-spacing: 0.02em;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .page-title span.icon {
      width: 32px;
      height: 32px;
      border-radius: 999px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: var(--primary-soft);
      color: var(--primary);
      font-size: 18px;
    }

    .page-subtitle {
      font-size: 0.95rem;
      color: var(--text-sub);
    }

    .logout-btn {
      padding: 6px 14px;
      border-radius: 999px;
      background: #ef4444;
      color: #fff;
      font-size: 0.85rem;
      font-weight: 500;
      border: none;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      box-shadow: 0 6px 14px rgba(239, 68, 68, 0.3);
      transition: background 0.12s ease, transform 0.1s ease, box-shadow 0.12s ease;
      white-space: nowrap;
    }

    .logout-btn:hover {
      background: #dc2626;
      transform: translateY(-1px);
      box-shadow: 0 8px 18px rgba(239, 68, 68, 0.4);
    }

    .logout-btn:active {
      transform: translateY(0);
      box-shadow: 0 4px 8px rgba(239, 68, 68, 0.25);
    }

    .summary-bar {
      margin-top: 10px;
      background: var(--primary-soft);
      border-radius: var(--radius-lg);
      padding: 12px 16px;
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
      gap: 8px;
      font-size: 0.98rem;
    }

    .summary-main {
      font-weight: 600;
      color: var(--primary);
    }

    .summary-time {
      color: var(--text-sub);
      font-size: 0.9rem;
    }

    .summary-time span {
      font-weight: 600;
      color: var(--text-main);
    }

    .class-list {
      margin-top: 22px;
      display: grid;
      grid-template-columns: 1fr;
      gap: 14px;
    }

    @media (min-width: 640px) {
      .class-list {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    .class-card {
      background: var(--card-bg);
      border-radius: var(--radius-md);
      padding: 14px 14px 16px;
      border: 1px solid var(--border);
      display: flex;
      flex-direction: column;
      gap: 10px;
      transition: transform 0.12s ease, box-shadow 0.12s ease,
        border-color 0.12s ease;
      cursor: default;
    }

    .class-card:hover {
      transform: translateY(-2px);
      border-color: #bfdbfe;
      box-shadow: 0 10px 18px rgba(15, 23, 42, 0.06);
    }

    .class-header {
      display: flex;
      justify-content: space-between;
      gap: 8px;
      align-items: flex-start;
    }

    .class-name {
      font-weight: 600;
      font-size: 1rem;
      color: var(--text-main);
    }

    .class-tag {
      font-size: 0.75rem;
      padding: 2px 8px;
      border-radius: 999px;
      background: #ecfdf5;
      color: #166534;
      border: 1px solid #bbf7d0;
      white-space: nowrap;
    }

    .class-meta {
      font-size: 0.86rem;
      color: var(--text-sub);
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .class-meta span {
      display: flex;
      align-items: center;
      gap: 4px;
    }

    .dot {
      width: 6px;
      height: 6px;
      border-radius: 999px;
      background: #22c55e;
      display: inline-block;
    }

    .class-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 4px;
      gap: 8px;
    }

    .status-text {
      font-size: 0.88rem;
      color: var(--text-sub);
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .status-text strong {
      color: #16a34a;
    }

    .btn-detail {
      border: none;
      border-radius: 999px;
      padding: 7px 14px;
      background: var(--primary);
      color: #ffffff;
      font-size: 0.88rem;
      font-weight: 500;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      box-shadow: 0 6px 12px rgba(37, 99, 235, 0.25);
      transition: background 0.12s ease, transform 0.08s ease,
        box-shadow 0.12s ease;
      white-space: nowrap;
      text-decoration: none;
    }

    .btn-detail:hover {
      background: #1d4ed8;
      transform: translateY(-1px);
      box-shadow: 0 8px 16px rgba(37, 99, 235, 0.3);
    }

    .btn-detail:active {
      transform: translateY(0);
      box-shadow: 0 4px 8px rgba(37, 99, 235, 0.18);
    }

    .btn-detail span.arrow {
      font-size: 14px;
    }
  </style>
</head>
<body>
  <main class="page">
    <header class="page-header">
      <div class="page-header-top">
        <h1 class="page-title">
          <span class="icon">📋</span>
          Điểm danh hôm nay
        </h1>

        <!-- Nút đăng xuất -->
        <form action="/auth/logout" method="POST">
          <button class="logout-btn" type="submit">⎋ Đăng xuất</button>
        </form>
      </div>

      <p class="page-subtitle">
        Theo dõi nhanh tình trạng điểm danh của các lớp học trong ngày.
      </p>
    </header>

    <section>
      <div class="class-list">
        <!-- Card lớp 1 -->
        <article class="class-card">
          <div class="class-header">
            <div>
              <div class="class-name">Lớp 12A1 - Toán</div>
            </div>
            <div class="class-tag">Đang diễn ra</div>
          </div>
          <div class="class-meta">
            <span>⏰ 07:00 - 07:45</span>
            <span>📍 Phòng: 301</span>
          </div>
          <div class="class-footer">
            <div class="status-text">
              <span class="dot"></span>
              <span><strong>2/2</strong> có mặt</span>
            </div>
            <a class="btn-detail" href="/camera/class">
              Xem chi tiết
              <span class="arrow">›</span>
            </a>
          </div>
        </article>

        <!-- Card lớp 2 -->
        <article class="class-card">
          <div class="class-header">
            <div>
              <div class="class-name">Lớp 12A2 - Văn</div>
            </div>
            <div class="class-tag" style="background:#f9fafb;border-color:#e5e7eb;color:#4b5563;">
              Chưa bắt đầu
            </div>
          </div>
          <div class="class-meta">
            <span>⏰ 08:00 - 08:45</span>
            <span>📍 Phòng: 302</span>
          </div>
          <div class="class-footer">
            <div class="status-text">
              <span class="dot" style="background:#9ca3af;"></span>
              <span><strong>0/2</strong> đã điểm danh</span>
            </div>
            <a class="btn-detail" href="/camera/class">
              Xem chi tiết
              <span class="arrow">›</span>
            </a>
          </div>
        </article>

        <!-- Có thể thêm các card khác ở đây -->
      </div>
    </section>
  </main>
</body>
</html>
